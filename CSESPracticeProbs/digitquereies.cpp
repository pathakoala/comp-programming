#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
int main() {
    ll t;
    scanf("%lld", &t);
    for(int i = 0; i<t; i++) {
        ll in;
        scanf("%lld", &in);
        printf("%lld", checker(in));
    }
}

ll checker(int i) {
    
}