#include<bits/stdc++.h>
using namespace std;

int main() {
    freopen("reduce.in", "r", stdin);
    freopen("reduce.out", "w", stdout);

    int n;
    cin >> n;
    vector<pair<int, int>> cords;
    for(int i = 0; i<n; i++) {
        int x, y;
        cin >> x >> y;
        cords.push_back({x,y});
    }
    
}